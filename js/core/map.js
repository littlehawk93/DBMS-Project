/*
*maps.js handles the canvas that holds the map of the USA
*/

$(document).ready(function() {
	/*var canvas = document.createElement('canvas');
	canvas.id = "canvas";
	canvas.width = 800;
	canvas.height = 300;
	canvas.style.backgroundImage = ("url(img/usa.png)");
	canvas.style.border = "ridge";
	$("#map_div").append(canvas);*/
});
